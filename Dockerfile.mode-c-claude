FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir \
    claude-agent-sdk \
    fastapi>=0.115 \
    uvicorn[standard]>=0.34 \
    httpx>=0.27
COPY mode-c-claude-sdk-main.py /app/main.py
ENV PYTHONPATH=/app PORT=8080
EXPOSE 8080
CMD ["python", "main.py"]

apiVersion: v1
kind: ConfigMap
metadata:
  name: forgeos-config
  labels:
    app.kubernetes.io/name: forgeos
    app.kubernetes.io/component: config
data:
  COMPANY_ID: "leadforge"
  # Disable demo HITL seed in cluster by default
  FORGEOS_SEED_HITL: "0"
  PORT: "5000"
  PYTHONUNBUFFERED: "1"

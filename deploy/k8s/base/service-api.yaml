apiVersion: v1
kind: Service
metadata:
  name: forgeos-api
  labels:
    app.kubernetes.io/name: forgeos
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: forgeos
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 5000
      targetPort: http
